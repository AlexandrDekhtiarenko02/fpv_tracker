# Ставится через pip (см. setup_pi.sh). Всё остальное — из apt, потому что
# picamera2/libcamera на Raspberry Pi собраны под конкретную сборку системы
# и pip-версии с ними не стыкуются.
#
# Версии зафиксированы по рабочему борту (Monolith, Zero 2W, Raspbian 13):
# «работает у меня» должно воспроизводиться на новой малине, а не «поставим
# что дадут сегодня».
opencv-contrib-python==4.13.0.90
pyserial==3.5
